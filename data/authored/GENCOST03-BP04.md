# GENCOST03-BP04

- **Key:** `GENCOST03-BP04`
- **Date:** 2026-06-05
- **Outcome:** not_promoted

---

- 2026-06-05 | GENCOST03-BP04 (whole BP — "Annotate user input to enable cost-aware content filtering"; AWS verbatim fetched 2026-06-05, 7 steps, risk Medium) | not_promoted — no principle authored. The BP directs you to wrap only untrusted user spans in Bedrock `<amazon-bedrock-guardrails-guardContent_xyz>` tags (random per-request `tagSuffix` to block seam-forging injection) so the **input content filter** evaluates the user input alone rather than the whole assembled prompt (system + retrieved context + history), shrinking the moderation-pass token bill; AWS explicitly notes input tags are NOT supported by the ApplyGuardrail API, so the filtering must be done application-side. Walked interactively with the user 2026-06-05. Two things surfaced: (1) once the Bedrock tag mechanism is stripped, the residual architectural mandate is thin — "scope the content filter to the untrusted input span" — a runtime prompt-assembly detail that does not survive extraction as a durable, CI-gateable artefact distinct from the existing P52 siblings (GC3B1-01 size, GC3B3-01 cacheability); (2) the cost-vs-safety tension initially raised (mis-tagging leaves untrusted content unscanned) dissolved on inspection — input content filters are span-local classifiers, so scanning the user span alone loses nothing for standard content-policy categories, and relational checks (grounding/faithfulness) are a different guardrail type that input tagging doesn't touch. Net: a vendor-specific cost micro-optimisation, not an architecture principle. **User rejected it as a non-principle on 2026-06-05.** step_promotion not formally scored — the user made the call directly; the rubric outcome would be not_promote on has_enforceable_artefact (thin) + not_vendor_menu grounds. Steps 4–6 (minimalist response scheme + hard response-length cap) are output-side concerns deferred to BP02 (Control model response length) / GC3B1-01's output budget. GENCOST03 closed: BP01 → GC3B1-01; BP02 → UNMAPPED (open sibling); BP03 → GC3B3-01; BP04 → not_promoted.
