# GENREL03-BP01

- **Key:** `GENREL03-BP01`
- **Date:** 2026-06-15
- **Outcome:** promoted → GR3B1-01

---

- 2026-06-15 | GENREL03-BP01 (AWS verbatim "Use logic to manage prompt flows and gracefully recover from failure" — "Create layers of abstraction between users and models to facilitate retries, error handling, and graceful failures... verify your model's respond with expected content... Define a fallback action or default modality in the absence of relevant data... have the agent classify responses from external systems as actionable or not... This method may serve to reduce non-determinism and increase reliability of agent workflows."; steps: (1) error classification, (2) recovery mechanisms — retries with exponential backoff, fallback templates, circuit breakers, (3) monitoring recovery rates, (4) continuous improvement; risk **Medium**) | **promoted_to_principle: GR3B1-01** — Recover gracefully when a model call goes wrong. **22nd standard; FIRST Reliability standard; opens pillar P3 — Reliability and focus area P31 — Prompt Remediation & Recovery.** Origin: surfaced from the "reason about reliability and cost like a Staff engineer" coverage audit (of the four concerns — non-determinism, retries, latency, token cost — token cost was fully covered (GC3 family), non-determinism only as an eval/drift discipline (GO1B1 family), retries + latency unwalked). The runtime face of non-determinism ("Inconsistent model performance — variations in output for similar inputs", named under the Reliability pillar's common challenges) AND retries both land here — two of the four concerns collapse into one walk. Anchored whole-BP (implementation_step null): steps 1-2 are the concretised core; step 3 monitoring rides GO3B2-01's emit channel; step 4 is continuous-improvement process advice with no commitable artefact. Contract: per-call recovery spec (`prompts/<name>/recovery.yaml` — output_schema + retry (retryable classes + backoff) + fallback (default / degraded / route-to-human)) + central wrapper (`call_model` on the central LLM SDK, GO3B1-01's substrate; validates, retries with backoff, returns fallback on exhaustion, idempotency key for side-effecting retries) + two pre_merge lints (declaration completeness; routed-execution AST — the GO3B1-01 no-inline pattern on the call plane) + quarterly recovery-effectiveness review over GO3B2-01-emitted fallback/parse-failure/retry telemetry. step_promotion 3/3/3/3 (artefact: recovery spec + wrapper + 2 lints; distinct: recover-from-a-bad-call face unowned — GS2B1-01 governs output SAFETY not structural usability/recovery, GC5B1-01 caps agent RUN LENGTH not single-call failure; in scope: GENREL03 is literally "remediation for loops, retries, failures"; vendor menu stripped: Bedrock Flows iterator/condition nodes — the surviving GenAI-distinct rule is validating non-deterministic output, which base-WAF REL05-BP01 graceful degradation does not cover). HALT observed (interactive walk: user pressed on whether structured-output/JSON mode already solves this — it does not, it is one half of the validate step and misses transient failures/truncation/refusals/empty-retrieval/semantic-wrongness; and how a central team monitors this — owns the wrapper, enforces routing via AST lint, reads emitted fallback-rate telemetry). Fields: applicability { llm, rag, agentic } all mandatory (ml omitted — classical ML returns no free-form non-deterministic content that fails to parse); serving_paradigm all four; maturity_level foundational (pays off at project #1; per-PR repo-local lint core; central SDK soft dep); impact_level Medium (matches AWS risk; broken/halted request or silent gap, workload-scoped); ownership enterprise via D1=no / D2=3 (the tier rubric's own "Centralised LLM SDK" calibration example) / project_architect / self_attestation_with_mechanical_evidence / dashboard_and_spot_check; dependencies GO3B1-01 soft + GO3B2-01 soft; references Air Canada chatbot + the AWS BP page; aigp IV.C provisional/unverified; eu_ai_act omitted (Security-only so far). RI at data/ri/GR3B1-01/README.md (Option A). Enforcement limit appended to enforcement_limits.md (lints prove declared+routed, not that the fallback is appropriate or the schema correct). See decisions.md (latest 2026-06-15).
