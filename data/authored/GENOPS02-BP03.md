# GENOPS02-BP03

- **Key:** `GENOPS02-BP03`
- **Date:** 2026-06-16
- **Outcome:** not_promoted

---

- 2026-06-16 | GENOPS02-BP03 (whole BP — live AWS title "Implement rate limiting and throttling to mitigate the risk of system overload"; verbatim re-fetched 2026-06-16) | not_promoted / absorbed + cross-pillar — no principle authored. **Closes GENOPS02** (3/3 BPs not_promoted). Mapping title corrected from "Implement solutions to mitigate…" → live "Implement **rate limiting and throttling** to mitigate…". BP mandate: throttling mechanisms + API Gateway rate limiting (control request volume); exponential backoff with jitter for transient errors; AWS SDK built-in retry; retry logic with idempotent operations where possible; Step Functions for complex retry workflows; circuit breaker patterns (fail fast on repeated failures); CloudWatch observability. Key context: "the level of parallelism may be constrained by the source system's capacity" — respect downstream limits. **User directed not_promote 2026-06-16** after a HALT (candidate: "Stay within the limits of what you and your model provider can absorb"). This is classic resilience engineering, already placed across the catalogue: (a) **retries / exponential backoff / circuit breakers / idempotent operations → GR3B1-01** (the first Reliability standard, authored 2026-06-15 — its recovery contract specifies "retries with exponential backoff, fallback templates, circuit breakers" and an idempotency key for side-effecting retries; this BP's retry half IS that contract); (b) **generic throttling / API Gateway rate limiting / Step Functions retry orchestration → base-WAF Reliability (REL05** client-throttle/backoff/retry); (c) the one GenAI-distinct slice — respecting **model-provider TPM/RPM throughput quotas** under parallelism — has a dedicated, still-unwalked home at **GENREL01 (Manage throughput quotas)**, where a throughput-quota principle (if any) should anchor, NOT under a GENOPS Ops-monitoring focus area. step_promotion 3/3/3/3 → not_promote (has_enforceable_artefact 3: retry/backoff/CB artefact owned by GR3B1-01, generic throttling has no GenAI-distinct artefact, provider-quota slice is cross-pillar; architecturally_distinct 3: GR3B1-01 + base-WAF REL05 + GENREL01 named as owners/target; in_bp_scope 3: substance is Reliability — cross-pillar to GENREL01 + GR3B1-01 in GENREL03 — not Operational Excellence monitoring; not_vendor_menu 3: API Gateway / Step Functions / SDK retry / CloudWatch menu, surviving generic rule is base-WAF REL05). **GENOPS02 CLOSED — 3 BPs, zero principles** (BP01/BP02/BP03 all not_promoted/absorbed). The operational-health question adds no GenAI-distinct enforceable architecture beyond the GO3B2 observability family, GO1B1-04 drift, and the GR3B1-01 / GENREL resilience family. See decisions.md (latest).
