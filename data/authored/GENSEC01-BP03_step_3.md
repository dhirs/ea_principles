# GENSEC01-BP03 step 3

- **Key:** `GENSEC01-BP03 step 3`
- **Date:** 2026-06-10
- **Outcome:** promoted → GS1B3-02

---

- 2026-06-10 | GENSEC01-BP03 step 3 (AWS verbatim "Develop a data ingestion pipeline which obfuscates or removes data that should not be processed by a foundation model. Examples of this data might be personal information. The scope of this data is informed largely by the workload use case." + sub-bullets: the customer-service example — handbooks/documentation yes, company financials/staff information/HR policies no — and "Sanitizing for prohibited material should happen before the model accesses the data, at time of ingestion."; risk **High** at BP level) | **promoted_to_principle: GS1B3-02** — Never ingest what the model should never process. **Sixth Security standard; 21st in the catalogue. Resolves the PII-removal deferral parked at the 2026-06-07 GS1B3-01 walk; first one-BP-two-standards case** (GS1B3-01 from step 4 = WHO retrieves, read gate; GS1B3-02 from step 3 = WHAT enters the store, write gate — access control cannot compensate for over-ingestion: a correctly ACL'd store still leaks PII to everyone authorized on the containing document). The earlier 'no Lens home' framing was wrong — step 3 itself is the anchor. Contract: sanitisation manifest (`ingestion/sanitisation/config.yaml` — data-card-derived exclusion classes (PII, credentials, prohibited, out-of-remit), per-class detector/action (mask/drop/human_review)/threshold, audit destination, waivers) + sanitisation stage wired AHEAD of the store on every ingestion path (writes only through the central ingestion SDK — the write-side sibling of GS1B3-01's retrieval SDK; detectors run before embedding, then GS1B3-01's ACL labelling: first whether, then who) + three pre_merge lints (declaration completeness; data-card coverage; routed-ingestion AST lint — direct store writes fail, the no-inline pattern on the write path, so a later-added nightly sync cannot bypass) + quarterly seeded-PII audit (canary docs with planted sensitive content, leak-rate vs threshold, exclusion list vs data card, on GO1B1-01's harness). step_promotion 3/3/3/3 (artefact: manifest + AST lint + audit records; distinct: write path unowned — GS1B3-01 read gate, GS6B1-01 training-corpus twin, GS2B1-01 output backstop not substitute; in scope: step native to BP03's FM-data-store question; vendor menu stripped: Bedrock/SageMaker guardrails, OpenSearch, Knowledge Bases). HALT waived — user-directed promote-and-complete (deferral discussed twice in-session). Fields: applicability { rag mandatory, agentic mandatory } (no model-accessible store = not applicable, omission per GS5B1-01 shape); serving_paradigm all four; maturity foundational; impact High; ownership enterprise (D1 no-borderline — privacy regulators push toward yes / D2=3 central detector library + ingestion SDK + canary corpus) / project_architect / self_attestation_with_mechanical_evidence / dashboard_and_spot_check; dependencies [{GS1B3-01 soft},{GO1B1-01 soft}]; references OWASP LLM02:2025 Sensitive Information Disclosure + Wiz/Microsoft 38TB AI-pipeline exposure; AIGP IV.C provisional (flagged: strongest family candidate to move under a privacy/data-governance competency). Enforcement limit in enforcement_limits.md (lints prove declared/covered/wired/logged, not detector recall — vacuous detector passes; efficacy → seeded-PII audit). RI at data/ri/GS1B3-02/README.md (Option A). not_prompted.json deferred entry removed. OPEN: JSON-parse verify from terminal; S3 re-upload; AIGP review. **GENSEC01-BP03 now fully resolved — no open security-pillar residue.**
