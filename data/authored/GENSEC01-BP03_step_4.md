# GENSEC01-BP03 step 4

- **Key:** `GENSEC01-BP03 step 4`
- **Date:** 2026-06-07
- **Outcome:** promoted → GS1B3-01

---

- 2026-06-07 | GENSEC01-BP03 step 4 (AWS verbatim "Create least-privilege access policies for foundation model and generated AI workloads. This Policy Document should contain resource identifiers granting explicit access to specific data in the vector datastore.") | **promoted_to_principle: GS1B3-01** — Never retrieve what the user isn't allowed to see. **First Security-pillar principle**; first under GENSEC; 16th in the catalogue. Anchors step 4, absorbs step 1 (classify data → the acl label) + step 2 (role-based store access), operationalises the BP's metadata-filtering implementation-guidance line. Concretises the one GenAI-distinct, sibling-unowned slice of BP03 — **retrieval-time per-query authorization** over a shared vector store — after BP01/BP02 were not_promoted as generic base-WAF IAM/network. Distinct because only RAG has a retrieval step: IAM gates store *reachability* not per-chunk *retrieval*; model-layer classification acts after retrieval; embedding strips source-system permissions so an unfiltered top-k over a shared store hands users chunks they could never read directly (OWASP LLM cross-tenant / broken access control). Mechanism: acl label on every document at ingestion + a central retrieval SDK that derives the caller's entitlements from the IdP and applies a pre-filter (workload passes identity, never the filter) + a pre-merge AST lint banning direct store-client calls against multi-tenant stores (GO3B1-01 no-inline pattern) + SDK version floor. **Honest enforcement limit (user-emphasised):** the lint enforces the plumbing (identity-scoped retrieval + labels present), NOT label correctness — a document mislabelled `public` is served faithfully to everyone — so a quarterly security label-audit is the named human control; recorded in the new data/enforcement_limits.md. step_promotion 3/3/3/3. Ownership: first **D1=yes** (cross-tenant/PII legal exposure → mandatory_centralise → enterprise) and first **audit_mode central_review_at_gate** (validator enterprise_security, evidence block populated). applicability { rag, agentic } (llm/ml omitted — no retrieval); serving_paradigm all four; maturity scaling; impact High; dependencies [{ GO3B2-01: soft }]; references 3 real (ragaboutit / we45 / Kiteworks). Steps 5 (curated-prompt access testing = GO1B1 harness) + 6 (training-data governance) not_promoted; step 3 PII-removal half deferred to a future GENSEC data-protection walk. RI at data/ri/GS1B3-01/README.md (Option A; Option B = source-system permission mirroring, additive). GENSEC01-BP04 (access monitoring) still UNMAPPED.
