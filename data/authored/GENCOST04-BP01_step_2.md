# GENCOST04-BP01 step 2

- **Key:** `GENCOST04-BP01 step 2`
- **Date:** 2026-06-06
- **Outcome:** promoted → GC4B1-01

---

- 2026-06-06 | GENCOST04-BP01 step 2 (AWS verbatim "Embed data using the smallest vector length. You may have to modify the chunk size of the document or introduce overlapping chunks to maintain high relevance on output.") | promoted_to_principle: GC4B1-01 — Use the smallest embedding vector that still retrieves well. First principle under GENCOST04 / GenCost Q4; first in the new focus area P53 — Cost-informed Vector Stores. 14th principle in the catalogue. Concretises the advice ("use a smaller vector") into an enforceable contract: a declared per-vector-store record (`embedding_model` + `vector_dimension` + `quality_floor` + `quality_result` + `checked_on` in `vectorstore/embedding.yaml`) + a `vectorstore/dimension_check` lint enforcing (1) declaration completeness, (2) deployed-index-vs-declared-dimension consistency by parsing the index IaC/config, (3) dimension-inflation governance requiring a recorded rationale for any upward change. The gate enforces that a quality number is RECORDED and the deployed dimension is real/stable; it does NOT re-run retrieval evaluation (that leans on a retrieval-eval harness — GENPERF04 territory). Option B (measured-from-retrieval-eval: gate the chosen dimension against the smallest clearing the quality_floor across a harness sweep) documented as an additive alternative; RI and gates built on Option A. step_promotion 3/3/3/3 (enforceable declaration + consistency/governance gates; architecturally distinct from GC1B1-01 — cost lever is the vector store storage/index/query, dimension is independent of model choice via Matryoshka/MRL, and largely irreversible once indexed; BP literally "Reduce vector length on embedded tokens" under GENCOST04 "Cost-informed vector stores"; generic dimension mandate, compressed-vector-types aside stripped). dependencies: GC1B1-01 soft (the embedding-model choice GC1B1-01's ADR records bounds the dimension range; the gate operates on the vector-store config independently) — parallel to GC2B2-01's soft dep on GC1B1-01. references (schema v1.11): Towards Data Science (vector-store cost linear in dimension; dimension reduction cuts infra cost up to 80% with quality held) + MindStudio/Gemini Embedding 2 (3,072 truncatable to 768/256 without significant retrieval-quality drop). ownership.tier enterprise via D1=no / D2=2 → recommended_centralise; validator project_architect, audit_mode self_attestation_with_mechanical_evidence. applicability { rag mandatory, agentic mandatory, ml nice_to_have } — llm OMITTED (pure LLM-only has no vector store; first cost principle to narrow applicability by omission). serving_paradigm all four mandatory (vector-store + embedding-output cost exist on every paradigm). maturity scaling (deployed-vs-declared dimension lint amortises at project #2; lighter than GC2B2-01's mature because it reads a single scalar). impact Medium. STATEMENT TITLE kept deliberately plain per the user's working-style rule committed to data/CLAUDE.md this session (plain spoken titles, no stacked clauses) — trades the statement rubric's names_artefact_and_enforcement dimension down (scored 1) in favour of the explicit user override; artefact + enforcement carried in statement.description. AIGP III.B unverified (III.A "Govern Data" noted as alternative). RI at data/ri/GC4B1-01/README.md (Option A). Step 1 absorbed.
