# GENREL03-BP02

- **Key:** `GENREL03-BP02`
- **Date:** 2026-06-22
- **Outcome:** promoted → GR3B2-01 (parked 2026-06-15, unparked 2026-06-22)

---

- 2026-06-22 | GENREL03-BP02 (whole BP — "Implement timeout mechanisms on agentic workflows"; verified 2026-06-15 from genrel03.html, live BP step page not re-fetched this session — reconstructed) | **promoted_to_principle: GR3B2-01** — Don't let a stalled agent hang forever. **24th standard; second under GENREL03 (focus area P31); the reliability twin of the run-length cap GC5B1-01.** Parked 2026-06-15 ("do BP01 only"); **unparked and promoted 2026-06-22** as the GENREL pillar was walked to completion. **HALT (user-driven):** (1) STANDALONE vs FOLD INTO GC5B1-01 — user directed standalone (GR3B2-01) over adding a 4th limit dimension to GC5B1-01; the failure mode (an I/O hang, not a runaway loop) is genuinely distinct and Reliability owning its own timeout standard reads cleaner than burying it in a cost-framed principle; shared run-config artefact + harness recorded as a soft GC5B1-01 dependency. (2) THE DISTINCT FAILURE — GC5B1-01 caps iterations/steps/tokens and is structurally blind to an agent idle on an unresponsive tool (no steps/tokens accrue while it waits); a wall-clock deadline fires regardless. (3) IDEMPOTENCY — user pressed: a run timing out at tool 10 has already fired tools 1-9; naive retry re-fires their side effects. Concluded timeout alone is unsafe. Scope decision (user accepted "Option 1"): keep GR3B2-01 NARROW — deadline + graceful termination + DLQ + safe-termination defaulting to NO auto-retry; tool-call idempotency split out as a SEPARATE not-yet-authored standard (GR3B1-01's retries need it too); when it lands, the safe-termination lint relaxes to "retry allowed only if all tool calls idempotent". (4) ARTEFACT LOCATION — user asked where the client declares this: NOT the prompt YAML (GO3B1-01 is per-template, wrong granularity) but the AGENT RUN CONFIG, the same artefact GC5B1-01's caps use; per-tool timeouts on each tool binding. (5) ENFORCEMENT — three pre-merge lints (declaration completeness, safe-termination, routed-execution AST) + quarterly timeout-effectiveness review over GO3B2-01 telemetry. **Distinctions:** GC5B1-01 = run LENGTH cap (blind to I/O hang); GR3B1-01 = recover a call that RETURNS an error (this fires when nothing returns); base-WAF REL05-BP05 = per-call timeout (cannot bound a model-chosen run that never returns). step_promotion **3/3/3/3**. **ENFORCEMENT LIMIT** (enforcement_limits.md): lints prove DECLARED + APPLIED + UN-BYPASSED + RETRY-SAFE BY DECLARATION; not that the deadline fires under load, is right-sized, or that cleanup is clean — runtime efficacy → quarterly review. CONTRACT (Option A): `wall_clock_deadline` + per-tool `timeout` + `on_timeout` (`terminate`/`dead_letter`) in the agent run config; central agent harness `run(...)` applies it by construction, terminates + cleans up + routes to DLQ on expiry. Fields: pillar P3 Reliability; focus_area P31 (shared with GR3B1-01); impact_level **High** (matches AWS BP risk High — a hung run exhausts the worker pool → service outage; unsafe-retry can double-fire a side effect); applicability { agentic mandatory, llm/rag nice_to_have, ml omitted }; serving_paradigm all four mandatory; maturity_level foundational; ownership enterprise (D1=no / D2=3 central agent harness) / project_architect / self_attestation_with_mechanical_evidence / dashboard_and_spot_check; dependencies [{ GC5B1-01 soft }, { GR3B1-01 soft }, { GO3B2-01 soft }]; references TWO (AWS BP page + base-WAF REL05-BP05); framework_mappings.aigp IV.C unverified; nist + eu_ai_act omitted (matching GR3B1-01). RI at data/ri/GR3B2-01/README.md (Option A). Also absorbs GENCOST05-BP01 step 3's deferred tool/Lambda timeout residue. JSON parses (count 24); schema-presence checked (field set identical to GR3B1-01). Catalogue now **24 standards** (9 GENOPS + 7 GENCOST + 6 GENSEC + 2 GENREL).

- 2026-06-15 | GENREL03-BP02 (AWS verbatim "Implement timeout mechanisms on agentic workflows" — detect and terminate long-running unexpected workflows; idle agents waiting on slow external systems; timeouts on functions/Action Groups accounting for cold starts; dead-letter queues; graceful termination + cleanup; risk **High**) | **parked — not authored, NOT formally not_promoted (user-directed "do BP01 only", 2026-06-15).** Two tensions held it from a same-session promote: (1) overlaps GC5B1-01 ("give every agent a limit it cannot run past") on a DIFFERENT axis — GC5B1-01 caps iterations/steps/tokens (cost-framed), BP02 caps wall-clock time (reliability-framed) — so it is either a standalone twin or a second facet of GC5B1-01; (2) base-WAF absorption risk — cross-refs REL05-BP05 (client timeouts), generic resilience engineering, the same shape that closed most of GENPERF, with only a thin GenAI veneer (agent idle-wait on a tool it does not know is slow + terminating sessions waiting on user input). Note: GENCOST05-BP01 step 3's deferred tool/Lambda/API timeouts were already flagged as belonging to "a future GENREL walk" — that residue lands here. RESOLVED 2026-06-22 → promoted to GR3B2-01 (see entry above).
