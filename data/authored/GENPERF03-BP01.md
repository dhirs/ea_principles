# GENPERF03-BP01

- **Key:** `GENPERF03-BP01`
- **Date:** 2026-06-07
- **Outcome:** not_promoted

---

- 2026-06-07 | GENPERF03-BP01 (whole BP — "Use managed solutions for model hosting, customization, and data access where appropriate"; AWS verbatim fetched 2026-06-07, 6 steps, risk **Medium**) | not_promoted — no principle authored. GENPERF03 ("How do you optimize computational resources required for high-performance distributed computation tasks?", focus area "Optimize consumption of high-performance compute") ships exactly ONE BP (BP01); walking it closes the focus area as a documented gap. The BP's mandate stripped of branding is "use managed solutions for hosting/customization/training where appropriate rather than carrying undifferentiated operational overhead, choosing by the level of control you need." **User directed not_promote 2026-06-07** after an interactive HALT. Grounds: (1) **Vendor menu.** Steps 2–5 are literally a service-selection tree — fully managed → Amazon Bedrock; more control → Amazon SageMaker AI endpoints; customization → managed customization workflows; training → SageMaker HyperPod (EKS/Slurm). This is the "consider X, use Y" shape the step_promotion `not_vendor_menu` dimension exists to catch; the cleanest vendor-menu BP walked to date. (2) **No enforceable artefact.** Step 1 ("determine the level of control your team needs") is decision-process advice (same shape as GENCOST01-BP01 step 1, GENPERF04-BP02 step 1); steps 2–5 carry no commitable pre-merge artefact; step 6 ("provide hosted models data access using a robust permissions model and federated data access") is GENSEC/IAM data-governance substance (cross-pillar), not performance. (3) **Narrow GenAI relevance.** The BP only bites for the minority of GenAI teams who **self-host their inference model** (e.g. Llama/Mistral on EC2+vLLM for cost-at-scale or data residency); the dominant RAG/agent pattern (managed generation API + API-or-local embeddings + a vector DB) already satisfies "use managed hosting" by default with nothing to gate. The promote case considered and rejected: a vendor-neutral "managed-by-default — declare the hosting paradigm, require a recorded ADR when self-managed is chosen over an available managed option" governance principle (the GC1B1-01 ADR pattern applied to build-vs-buy hosting). Rejected because it is close to the existing `serving_paradigm` axis, leans operational-governance rather than architecture, and the AWS BP offers almost nothing vendor-neutral to anchor it to. step_promotion would fail `not_vendor_menu` (~1) and `has_enforceable_artefact` (~1). **GENPERF03 focus area CLOSED** (one BP, not_promoted).
