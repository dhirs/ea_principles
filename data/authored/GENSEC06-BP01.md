# GENSEC06-BP01

- **Key:** `GENSEC06-BP01`
- **Date:** 2026-06-10
- **Outcome:** promoted → GS6B1-01

---

- 2026-06-10 | GENSEC06-BP01 (whole BP — "Implement data purification filters for model training workflows"; AWS verbatim fetched 2026-06-10, 6 steps, risk **High**) | **promoted_to_principle: GS6B1-01** — Never train a model on data no one has examined. **Fifth Security-pillar principle; 20th in the catalogue (9 GENOPS + 6 GENCOST + 5 GENSEC).** GENSEC06 ("How do you detect and remediate data poisoning risks?", focus area "Data poisoning") ships exactly one BP, so this closes the focus area — and with it the GENSEC pillar: GENSEC01–06 all walked and closed, 5 promoted. Anchored whole-BP (implementation_step null): the mandate lives in the implementation guidance ("Data should be examined and cleaned for content which may be considered poisonous before introducing that data to a training job"); steps 2+3 carry the promotable core (policy-derived filter declaration + filters for biased / factually incorrect / hateful / violent / irrelevant content), step 1 is observation input, step 5 is the wired pipeline stage, steps 4/6 are consider-advice absorbed as filter options (guardrail engine as one filter implementation; relevance as one declared category). Concretises training-data purification — **the LEARNING-side member of the Security guardrail family** (GS4B2-01 input, GS1B3-01 retrieval, GS2B1-01 output, GS5B1-01 action; this = what the model LEARNS — the only face whose failure is irreversible by rollback: poison absorbed into weights is retrained away or the model retired). Contract: purification manifest (`training/purification/config.yaml` — categories derived from the org AI policy / data cards per AWS step 2, each with filter + threshold + on_flag (quarantine/drop/human_review), audit destination, explicit waivers) + the purification stage wired AHEAD of the job (the training/customization job — SageMaker / k8s / Slurm / managed vendor fine-tune API — consumes only the stage's output, never a raw source; every run writes an audit record: dataset hash, filter versions, scanned/flagged/quarantined counts) + three pre_merge lints (declaration completeness; policy coverage — required category screened or explicitly waived; source coupling — raw-source reference fails, dataset-version changes require a matching audit-record hash) + a quarterly seeded-poison audit (canary corpus across declared categories through a test run, catch-rate vs threshold, category list vs current AI policy, on GO1B1-01's harness). New focus area **P25 — Training Data Integrity**. step_promotion 3/3/3/3 (enforceable artefact: manifest + 3 lints + audit records; architecturally_distinct: the training-data path is unowned — all four GS siblings govern inference-time faces, GO1B1 owns evals not ingestion; in_bp_scope: GENSEC06 is literally the data-poisoning question, single BP; not_vendor_menu: strip Transcribe Toxicity Detection / Bedrock Guardrails API / fmeval / SageMaker Studio eval / HyperPod EKS/Slurm and the no-unscreened-data-reaches-a-training-job rule survives). DECISIONS (HALT presented statement + problem sketch + four discussion points; user directed promote-and-complete): (1) PROMOTE despite narrow applicability — unlike the GENOPS04-BP02 / GENPERF03-BP01 not_promotes (vendor menus on top of narrow), a vendor-neutral CI-gateable artefact survives extraction cleanly; narrowness carried honestly by the statement's scope. (2) APPLICABILITY all four patterns mandatory — the applicability axis is pattern-keyed, not has-training-path-keyed; fine-tuning is available to every pattern (incl. pure-API via vendor fine-tune APIs, which the source-coupling gate covers); no-training-path workloads satisfy vacuously (the GS5B1-01 no-action-surface shape). Schema wrinkle flagged in the HALT, resolved without schema change. (3) GENSEC01-BP03 PII slice kept SEPARATE — retrieval corpus (inference-side) ≠ training corpus; still an open extension candidate with no Lens home (confirmed by the full pillar walk: GENSEC has no data-protection focus area). (4) Post-training toxicity eval (the BP's fmeval sentence) absorbed by GO1B1 family; training-environment isolation = base-WAF Security. (5) ENFORCEMENT LIMIT (data/enforcement_limits.md): lints prove filters DECLARED, POLICY-COVERED, WIRED, LOGGED — not that they CATCH poison (a vacuous filter passes); efficacy → the quarterly seeded-poison audit. dependencies [{GO1B1-01 soft — audit rides the harness},{GS2B1-01 soft — guardrail configs reusable as data-layer filters per step 4}]; serving_paradigm all four (training path orthogonal to serving); maturity foundational (poison at project #1 is locked into weights; per-PR repo-local lints, no deployed substrate); impact High (matches AWS risk; retrain + safety/brand/regulatory exposure); ownership enterprise (D1 no-borderline / D2=3 — central filter library, data-layer guardrail adapters, canary corpus, audit schema, per-orchestrator stage templates) / project_architect / self_attestation_with_mechanical_evidence / dashboard_and_spot_check; references OWASP ML02:2023 Data Poisoning Attack (cited in AWS's own Resources) + Microsoft Tay (The Verge, 2016 — the canonical unscreened-learning withdrawal); AIGP IV.C provisional (flagged: may belong under a data-governance competency). RI at data/ri/GS6B1-01/README.md (Option A). OPEN: JSON-parse verify from terminal; frontend P25 render. **GENSEC06 CLOSED — 1 BP, promoted. GENSEC PILLAR FULLY WALKED.** See decisions.md (latest).
