# GENSEC01-BP01

- **Key:** `GENSEC01-BP01`
- **Date:** 2026-06-07
- **Outcome:** not_promoted

---

- 2026-06-07 | GENSEC01-BP01 (whole BP — "Grant least privilege access to foundation model endpoints"; AWS verbatim fetched 2026-06-07, 4 steps, risk **High**) | not_promoted — no principle authored. **First GENSEC walk.** GENSEC01 question stem verified ("How do you manage access to generative AI endpoints?", focus area "Endpoint security", 4 BPs). BP mandate: scope FM-endpoint access to least-privilege IAM roles (permission boundaries, trust-policy conditions, session durations); at the org layer use SCPs/RCPs to block models the org has not approved. **User directed not_promote 2026-06-07** after an interactive HALT in which the promote case was built and then collapsed by the user. Promote candidate: an identity-layer "approved-models + least-privilege endpoint" principle (declare approved FM-endpoint ARNs in `security/model-access.yaml` + org SCP/RCP denying `bedrock:InvokeModel` on non-approved ARNs + pre-merge lint on the workload IaC for ARN-scoped permissions / permission boundary / SCP existence), pitched as defense-in-depth distinct from GO1B1-06 (build-time/code vs runtime/identity, catching shadow-model calls). The user collapsed the distinctness: GO3B1-01's no-inline-call AST lint (inherited by GO1B1-06) already scans code for *direct* model calls (e.g. raw `boto3 ... invoke_model`), so the in-repo shadow-model hole is already caught; the residual gap (console/CLI, laptop notebook on a shared role, ungated repo, third-party SaaS) is "lock down the account" = generic base-WAF SEC03 IAM hygiene, not GenAI-distinct. AWS's own Resources confirm generic ownership — five base WAF Security cross-refs (SEC02-BP01/02/06, SEC03-BP01/02). Steps 1–3 textbook IAM; step 4 Q Developer subscription vendor-specifics. Only GenAI-distinct slice (approved-models) already owned by GO1B1-06 at the code layer. step_promotion would fail `architecturally_distinct` (generic IAM + GO1B1-06 overlap) + `not_vendor_menu`. Cleared stale provisional "possibly PRIN_003 (Centralized LLM SDK)" mapping (PRIN_003 is call-routing through a central SDK, not IAM endpoint permissions — mis-fit). **GENSEC pillar note:** its BPs lean on generic base-WAF Security discipline (SEC02/03 identity+permissions, SEC05 network) applied to a model endpoint; a GENSEC promote must clear a GenAI-distinct enforceable artefact the base WAF Security pillar does not own and existing siblings (GO1B1-06, GO3B1-01/GO3B2-01/02) do not cover.
