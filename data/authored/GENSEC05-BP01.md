# GENSEC05-BP01

- **Key:** `GENSEC05-BP01`
- **Date:** 2026-06-08
- **Outcome:** promoted → GS5B1-01

---

- 2026-06-08 | GENSEC05-BP01 (whole BP — "Implement least privilege access and permissions boundaries for agentic workflows"; AWS verbatim fetched 2026-06-08, 4 steps, risk **High**) | **promoted_to_principle: GS5B1-01** — Put every consequential agent action through a gate before it runs. **Fourth Security-pillar principle; 19th in the catalogue (9 GENOPS + 6 GENCOST + 4 GENSEC).** GENSEC05 ("How do you avoid excessive agency for models?", focus area "Excessive agency") ships exactly one BP, so this closes the focus area. Excessive agency is the OWASP Top-10 LLM threat — an agent taking actions beyond its intended purpose (not malicious; the agent has little knowledge beyond the prompt of what is permitted). Anchored whole-BP (implementation_step null): steps 1-3 are generic IAM least-privilege construction (scoped policies, resource ARNs, permission boundaries, trust-policy conditions — AWS Resources cross-ref SEC02-BP01/02/06 + SEC03-BP01/02, the same base-WAF BPs that drove the GENSEC01-BP01 not_promote); the GenAI-distinct mandate lives in the implementation guidance + step 4 (user confirmation for agent actions). Concretises agent ACTION authorization — the one GenAI-distinct, sibling-unowned slice: every agent declares a tool manifest (per tool `scope` + `class` read/write + a `gate` for writes — `confirm` for human-in-loop or `policy:<name>` deterministic for unattended), the agent loop executes tools only through a central wrapper, and three pre-merge lints enforce declaration completeness, routed execution (no tool reached except through the wrapper — the GO3B1-01 no-inline pattern on the action plane), and consequential-implies-gated (incl. unattended-write-needs-policy) + a quarterly agent red-team. **THE ACTION-side member of the Security guardrail family:** GS4B2-01 input, GS2B1-01 output, GS1B3-01 retrieval (read), GS5B1-01 = what the agent DOES (write); the fourth and last face of the agent surface, the only one that changes the world. New focus area **P24 — Agentic Action Control**. step_promotion 3/3/3/3 (enforceable artefact: tool manifest + 3 pre-merge lints + central wrapper; architecturally_distinct: action plane, unowned — input/output/retrieval each covered by a named sibling; in_bp_scope: BP literally least-privilege/permission-boundaries for agentic workflows under "avoid excessive agency"; not_vendor_menu: strip Bedrock agent execution roles / Flows service roles / Verified Permissions / Bedrock User Confirmation and the constrain-to-declared-tools + scope + gate-consequential-actions rule survives). DECISIONS (extended HALT — the user walked the normal agent paradigm: tool selection by description, where the agent sits in a chatbot flow, what is enforceable, standardisation, project obligations, and validity for unattended/triggered agents): (1) PROMOTE not not_promote — unlike GENSEC01-BP01 (pure generic IAM), this carries a GenAI-distinct slice: description-based tool selection is the model REASONING (soft, manipulable), the principle adds the AUTHORIZATION layer (hard boundary on what is reachable and what fires without a check) — different layers, conflating them is the failure. (2) SCOPE = the OWASP triad (functionality + permissions + autonomy): manifest caps functionality, execution role caps permissions, write-gate caps autonomy. (3) IAM half (steps 1-3) NOT re-linted — becomes the enforcement behind the manifest's `scope` (per-tool least-privilege execution role), depended on not reinvented. (4) GATE TWO FORMS — `confirm` (human) OR `policy:<name>` (deterministic), and the lint fails an unattended write gated only by `confirm`, forcing a policy gate — resolves the no-human-loop case. (5) POLICY SIGNATURE standardised to `(ctx) -> bool` so dispatcher/lint/red-team treat all policies uniformly; logic project-local. (6) ENFORCEMENT LIMIT (data/enforcement_limits.md): lints prove declared + routed + gated, NOT honest classification (write mislabeled read passes) or minimal scope (over-broad role passes) — efficacy → quarterly agent red-team + blast-radius review (adversarial action corpus, escape-rate vs threshold + classification/scope honesty, reuses GO1B1-01's harness). dependencies [{GO3B1-01 soft},{GO1B1-01 soft}]; applicability { agentic mandatory } ONLY (no action surface without tools; non-agentic LLM/RAG that call tools ARE agentic); serving_paradigm all four; maturity foundational (per-PR repo-local lint, no deployed substrate, pays off project #1; central agent SDK is a soft dep); impact High (cited Replit incident wiped production data for 1,200+ companies during a freeze); ownership enterprise (D1 no-borderline / D2=3 central agent SDK) / project_architect / self_attestation_with_mechanical_evidence / dashboard_and_spot_check; references Replit production-DB deletion (Fortune, Jul 2025) + OWASP LLM08:2025 Excessive Agency; AIGP IV.C provisional. Legacy PRIN_008 (Agent Security Framework) formally ruled out (historical-only; broad framing, not this specific action-authorization contract). RI at data/ri/GS5B1-01/README.md (Option A). OPEN: JSON-parse verify from terminal; frontend P24 render (npm run build from s3-json-viewer/). **GENSEC05 CLOSED — 1 BP, promoted.** See decisions.md (latest).
