# GENREL04-BP01

- **Key:** `GENREL04-BP01`
- **Date:** 2026-06-23
- **Outcome:** not_promoted

---

- 2026-06-23 | GENREL04-BP01 (whole BP — "Implement a prompt catalog"; focus area GENREL04 "How do you maintain versions for prompts, model parameters, and foundation models?"; live pages fetched 2026-06-23 — genrel04.html + genrel04-bp01.html; mapping_state **verified**; AWS risk **Medium**) | **not_promoted 2026-06-23.** First GENREL04 BP; the Reliability-framed twin of **GO3B1-01** that the lens_mapping pre-flag had earmarked. **The problem AWS poses:** a centralized versioned prompt catalog with rollback, a testing framework that releases a prompt to "live" once it passes thresholds, versioned hyperparameter ranges (temperature/top_p/top_k) validated against prompt versions, and prompt test results maintained against several model versions. **Why absorbed:** GO3B1-01 ("Treat prompts as governed assets") already mandates the catalog — registry per-row schema `id/version/body/variables/output_schema/model/runtime_token_budget/owner/status∈{draft,active,deprecated,archived}`, no-inline lint, central SDK. Step-map: step 1 (catalog structure — metadata schema, naming, access control) + step 2 (version control, changelog, rollback = revert to prior `active`) + step 5 (governance — approval, audit, review) → GO3B1-01 verbatim; step 3 (testing → release-to-live on passing thresholds) → **GO1B1-01** eval harness (gates a change to production on known-good behaviour). 4 of 5 steps already discharged; AWS is restating GO3B1-01 under Reliability. **Residual slice (genuinely absent from GO3B1-01):** the row binds a single `model` + token budgets but NOT (a) a validated sampling-hyperparameter RANGE per prompt version, nor (b) a prompt×model-version test/compat matrix. This does NOT stand up a new standard — it splits: the *declaration* is a GO3B1-01 schema extension (optional `hyperparameter_ranges` + `model_compatibility` fields, existence-linted, SDK rejects out-of-range sampling the way it already enforces token budget); the *validation* ("validated against", per-model test results) is GO1B1-01's harness doing the running, no new gate. step_promotion **3/3/2/3 → not_promote** (has_enforceable_artefact 3 — the catalog artefact already exists as GO3B1-01's manifest+lint, no new commitable artefact; architecturally_distinct 3 — GO3B1-01/GO1B1-01 named precisely, residue named and routed; in_bp_scope 3 — Reliability-twin of an Ops standard, placement unambiguous; not_vendor_menu 2 — BP01 is generic mandate not a vendor menu, absorption is correctly the basis). **DELIVERABLE: ADR at data/adr/GENREL04-prompt-catalog.md** — recommends the GO3B1-01 two-field schema extension instead of a principle. No principle authored; catalogue stays at 24 standards. `principles.json` unchanged (schema-extension is a recommendation, not applied this walk).
