# GENREL05-BP02

- **Key:** `GENREL05-BP02`
- **Date:** 2026-06-27
- **Outcome:** not_promoted

---

- 2026-06-27 | GENREL05-BP02 (whole BP — "Replicate embedding data across all regions of availability"; live page fetched + verified 2026-06-27 from genrel05-bp02.html; mapping_state **verified**) | **not_promoted (user-directed 2026-06-27).** Second BP of GENREL05, AWS risk **Medium**. The BP is cross-Region replication of GenAI data stores (embeddings, knowledge bases) so models have local data in every serving Region: Amazon S3 cross-Region replication, Amazon OpenSearch Service cross-cluster replication, AWS Glue sync pipelines, with data-sovereignty/residency constraints and S3 Intelligent-Tiering cost optimisation. **No distinct enforceable AI-specific principle survives.** (1) Generic cross-Region data replication — "embedding store / vector store" is the GenAI-flavoured noun, but the mechanism is identical to replicating any datastore; (2) **vendor menu** (S3 CRR / OpenSearch cross-cluster replication / Glue), the not_vendor_menu shape; (3) **no commitable repo artefact or CI gate** — replication config is infra / landing-zone, not a workload-repo lint; (4) absorbed by base-WAF **REL10** (the BP cross-references REL04/07/10). The two adjacent angles considered at the HALT both land outside reliability: **data sovereignty / residency** of embeddings is a GDPR/compliance concern (owned by a future framework mapping + GS1B3-02's ingest gate governs WHAT enters the store, not its geographic replication), and **replication cost** is GENCOST territory — neither a reliability gate. step_promotion **3/3/3/3 → not_promote** (has_enforceable_artefact 3 — no repo artefact, replication is landing-zone; architecturally_distinct 3 — redundant with base-WAF REL10, named; in_bp_scope 3 — native GENREL05, sovereignty/cost angles named as cross-pillar; not_vendor_menu 3 — S3 CRR / OpenSearch / Glue menu quoted). No ADR (no AI-specific recommendation beyond "follow base-WAF REL10; respect data-residency"). No principle authored; catalogue stays at 25 standards.
