# GO2B3-01

- **Key:** `GO2B3-01`
- **Date:** 2026-06-23
- **Outcome:** promoted → GO2B3-01

---

- 2026-06-23 | GO2B3-01 (GENOPS02-BP03 "Implement solutions to mitigate the risk of system overload", implementation **step 4** — "Design retry logic: Implement idempotent operations where possible to facilitate safe retries"; live BP page re-fetched + verified 2026-06-23; AWS risk **High**) | **PROMOTED → GO2B3-01 "Make every side-effecting tool call safe to retry."** 25th standard in the catalogue; **first standard ever promoted from GENOPS02** (focus area closed 2026-06-16 with zero principles — now REOPENED with one) and the first standard under the newly-allocated focus_area **P12 — Operational Health Management**. **ORIGIN:** flagged during the GR3B2-01 walk (2026-06-22) as "a distinct, not-yet-authored standard" that both GR3B1-01's retries and GR3B2-01's safe-termination defer to; surfaced for a demo as the strongest pending promote (a standard two shipped standards already wait on) and authored at user direction. **HALT (user-driven):** (1) **Pillar/ID placement** — three options put to the user: GR3B2-02 (Reliability, beside the timeout twin), GR3B1-02 (Reliability, graceful-recovery), or GO2B3-01 (Op-Ex, anchoring GENOPS02-BP03 step 4 where "idempotent operations" is verbatim). **User chose GO2B3-01** — most honest AWS anchor — accepting it reopens a closed focus area and sits a pillar from its dependents. (2) **Distinct failure** — a recovery retry replays side-effecting tool calls that already ran, double-firing a real action (charge/email/order); distinct from GR3B1-01 (decides a retry happens) and GR3B2-01 (decides a run stops) — this decides a retry doesn't duplicate what occurred. (3) **Solution + enforceability** — walked PR-time vs runtime: PR-time lints prove every side-effecting tool declares an `idempotency_key` or `idempotent:false`, that unsafe tools aren't on a retry path, and that calls route through the harness; runtime = the central harness attaches the key by construction and refuses to auto-retry an unsafe tool. Client provides **config** (tool-binding declarations), not a dataset (that's GO1B1-01's eval harness). (4) **Telemetry** — user pushed back hard on any per-tool-call telemetry-emit requirement ("another headache"); **resolved NOT to mandate a telemetry pipeline** — the provider-honouring gap (does the downstream actually de-dup on the key?) is an **accepted, documented limit**, a deliberate divergence from GR3B2-01/GR3B1-01 which carry a binding quarterly telemetry review. GO2B3-01 rests on build-time lint + harness-by-construction only. **step_promotion 3/3/3/3** (has_enforceable_artefact 3 — tool-binding fields + 3 pre-merge lints, path-shaped binary gates; architecturally_distinct 3 — distinct from GR3B1-01/GR3B2-01, the safety property both defer to, named precisely; in_bp_scope 3 — anchors GENOPS02-BP03 step 4 verbatim, the BP's rate-limiting thrust stays not_promoted; not_vendor_menu 3 — strip Step Functions/API Gateway/SageMaker and a provider-neutral rule survives: a declared idempotency key on every side-effecting call, attached by construction, unsafe tools barred from auto-retry). **Fields:** pillar P1 — Operational Excellence; **new focus_area P12 — Operational Health Management** (mirrors AWS GENOPS02 "Monitor and manage operational health"; existing P1 slots P11 Eval, P13 Traceability, P12 was unused); applicability agentic mandatory / llm+rag nice_to_have / ml omitted; serving_paradigm all four mandatory; maturity foundational; impact_level High (double-fired charge = financial/data consequence, matches BP risk High); ownership.tier enterprise (D1=no/D2=3, central agent harness); dependencies GR3B2-01 soft (reciprocal — its safe-termination lint relaxes to allow auto-retry when all tools idempotent), GR3B1-01 soft (its side-effecting retries become safe), GO3B2-01 soft (OPTIONAL forensic duplicate-key signal, no new emitter); references = anchoring BP + AWS Prescriptive Guidance retry/backoff; framework_mappings = aws + **eu_ai_act Art 15 robustness** (added v1.0.1, unverified, control_ref null) — aigp omitted (no natural BoK v2.1 competency, same as GR3B1-01/GR3B2-01 cleared in the 2026-06-23 audit), nist omitted (unpopulated catalogue-wide). NOTE: eu_ai_act was initially omitted on sibling-consistency grounds; user correctly pushed back (consistency is not the catalogue's test — discharge is), and Art 15(4) resilience-to-faults is genuinely discharged by the idempotency gate, so it was added. GR3B1-01/GR3B2-01 likely warrant the same Art 15(4) ref — reliability-family EU AI Act pass logged as follow-up. **ENFORCEMENT LIMIT** appended to enforcement_limits.md — first catalogue case of a gap left as an *accepted limit* rather than backed by a periodic review (user-directed). **RIPPLE:** GENOPS02 reopened (lens_mapping BP03 row → partial-promote split, focus-area + pillar "fully walked" notes updated; GENOPS pillar 9 → 10 standards). **RECIPROCAL:** GR3B1-01 + GR3B2-01 get a change_history note that GO2B3-01 now exists (their retry/safe-termination logic may key off idempotency declarations); fully rewriting their gate text to cite GO2B3-01 is logged as follow-up. **DELIVERABLES:** principles.json (GO2B3-01 inserted at idx 6, GO-pillar numeric order; parses clean, count 25, all 21 universal keys present — schema-presence checked vs all priors; format_version unchanged 1.13, no schema change), RI at data/ri/GO2B3-01/README.md (Option A). Open items: S3 re-upload; reciprocal gate-text relaxation in GR3B2-01/GR3B1-01; GENREL05/06 still to walk.
