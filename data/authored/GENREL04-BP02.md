# GENREL04-BP02

- **Key:** `GENREL04-BP02`
- **Date:** 2026-06-23
- **Outcome:** not_promoted

---

- 2026-06-23 | GENREL04-BP02 (whole BP — "Implement a model catalog"; focus area GENREL04; live page fetched 2026-06-23 — genrel04-bp02.html; mapping_state **verified**; AWS risk **Low**) | **not_promoted 2026-06-23.** Second GENREL04 BP; the Reliability-framed twin of **GO1B1-06** ("Know which model you're running, and re-prove behaviour before you change it"). **The problem AWS poses:** a centralized model catalog storing model versions + model cards, supporting deploy/rollback to a given version, recording access/lineage/customizations, carrying model cards (capabilities, limitations, training-data characteristics, intended use, ethical considerations/biases). **Why absorbed:** GO1B1-06 already mandates the approved, immutable, versioned catalog — every model ref resolves to a `models/catalog.yaml` entry (exact immutable version: dated provider snapshot or self-hosted weights digest), floating aliases banned, pre-merge catalog-membership lint, re-eval gate on any version change. Step-map: step 1 (catalog structure) + step 2 (tracking — lineage/versions/customizations/benchmarks) + step 4 (governance — approval/deploy/monitor/security/usage) + step 5 (maintenance — update/deprecation/archival) + step 6 (validation — testing/acceptance/benchmarks/quality gates) → GO1B1-06 (+ GO1B1-01 harness, GO3B2-01 monitoring, base-WAF). 5 of 6 steps already discharged. **Residual slice:** step 3 (**model cards**) — capabilities/limitations, training-data characteristics, intended use/constraints, ethical considerations/biases — a governance-document artefact, NOT a reliability gate, and partly a **responsible-AI/GENSEC** concern (bias, intended-use constraints). Resolves to an optional `model_card` field on GO1B1-06's entry schema (existence-linted) + the responsible-AI substance flagged as a future **GENSEC/responsible-AI** candidate, not forced onto a reliability standard. step_promotion **3/3/2/3 → not_promote** (has_enforceable_artefact 3 — catalog already exists as GO1B1-06's `models/catalog.yaml`+lint; architecturally_distinct 3 — GO1B1-06 named precisely, model-card residue named/routed; in_bp_scope 2 — GENSEC destination named in the abstract; not_vendor_menu 3 — Bedrock Marketplace/SageMaker model catalog are Resources links only, steps are generic mandate). **DELIVERABLE: ADR at data/adr/GENREL04-model-catalog.md** — recommends the GO1B1-06 `model_card` field + GENSEC candidate. No principle authored; catalogue stays at 24 standards. **GENREL04 closes empty — both BPs not_promoted, two schema-extension ADRs.** `principles.json` unchanged.
