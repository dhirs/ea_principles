# GENOPS01-BP01 implementation-guidance directive

- **Key:** `GENOPS01-BP01 implementation-guidance directive`
- **Date:** 2026-06-04
- **Outcome:** promoted → GO1B1-06

---

- 2026-06-04 | GENOPS01-BP01 implementation-guidance directive (AWS verbatim "Run these evaluations when new candidate models are available, or when model customization techniques are applied. For example, fine-tuned and customized models should be subject to the same evaluation criteria and cadence as non-customized models.") | promoted_to_principle: GO1B1-06 — Pin every model call to an immutable, catalogued version and re-run the evaluation harness before any version change ships. This is the "model-change gate" earmarked in GENOPS01-BP01 step 3's not_promoted note. Concretises the AWS directive into: an approved model catalog (`models/catalog.yaml`, exact immutable versions, no aliases) + a pre_merge catalog-membership lint + a runtime-refusing model SDK (GO3B1-01 pattern applied to model identifiers) + a pre_merge gate coupling any pinned-model-identifier change to a mandatory GO1B1-01 re-run. Anchored to the BP's implementation-guidance directive, not a numbered step (implementation_step null); the seven numbered steps are already concretised by GO1B1-01..05. step_promotion 3/3/3/3 (enforceable artefacts + two gates; architecturally distinct from GO1B1-04 drift — discrete model-swap step-change vs gradual cumulative drift; in-bp-scope: BP guidance directs re-eval on model change; vendor tooling stripped). Distinct value: catches the silent model-substitution failure (provider rolls a floating alias forward / deprecates a snapshot / gateway default swap) that opens no PR, fires no per-PR or drift gate, and surfaces only as a customer-experience regression. ownership.tier enterprise via D1=no borderline (regulated MRM regimes push to mandatory_centralise) / D2=3 (model catalog + membership lint + runtime-refusing SDK + re-eval gate wiring + rollback tooling) → recommended_centralise. maturity scaling (needs the GO1B1-01 harness substrate + a deployed model; enforcement infra amortises at project #2). serving_paradigm all four mandatory (API workloads face provider roll-forwards/deprecations; self-hosted still need re-eval-on-change when new weights deploy). impact High. AIGP III.B (model lifecycle) unverified — deliberate shift from the GO1B1 family's III.A data-governance anchor. LENS GAP RECORDED: no dedicated AWS BP for model versioning (GENOPS03 names it in the question but ships only prompt-mgmt + tracing BPs); the model-registry twin of GO3B1-01 left as an open extension candidate in lens_mapping.md GENOPS03.
