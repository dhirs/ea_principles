# GENCOST03-BP02

- **Key:** `GENCOST03-BP02`
- **Date:** 2026-06-16
- **Outcome:** promoted → GC3B2-01

---

- 2026-06-16 | GENCOST03-BP02 (whole BP — "Control model response length"; BP title + GENCOST03 question confirmed via search, live step page not fetchable this session) | **promoted_to_principle: GC3B2-01** — Keep the cost of every response under deliberate control. **The output-side mirror of GC3B1-01 (input footprint); fourth GENCOST principle; 23rd in the catalogue.** Flagged as the most likely remaining promote candidate after the GENOPS pillar closed; picked up 2026-06-16. BP mandate: bound response length to reduce output-token cost (max_tokens, stop sequences, concise-output prompt instructions). **User directed PROMOTE 2026-06-16** after an extended HALT centred on enforceability. The user pressed hard — *how do you check response length at a CI gate?* — and the honest answer drove the design: **you can't**; the response does not exist at build time. So GC3B2-01 does NOT gate output length. It mandates that every template declare a real `runtime_token_budget.output` (GO3B1-01's blank-by-default field), the central SDK applies it as the call's `max_tokens` by construction, and two pre-merge lints enforce the wiring: a declaration lint (output budget present + non-zero) and an AST no-bypass lint (extending GO3B1-01's no-inline routing lint) that fails any call site passing its own output-cap parameter that bypasses the registered ceiling — a variable override fails outright, a literal override only allowed when ≤ the declared ceiling — plus a budget-inflation governance lint requiring a recorded rationale to raise a ceiling. **ENFORCEMENT LIMIT (the distinguishing point from GC3B1-01, whose input footprint IS statically computable):** the gate proves the cap is declared + applied + un-bypassed, NOT that the model halts at it under live load — the "wired-in, not runtime behaviour" limit, appended to enforcement_limits.md. Runtime length observed via Option C (GO3B2-01 per-call output-token signal); ceiling appropriateness via the Gate 2 inflation review. step_promotion 3/3/3/3 (has_enforceable_artefact: declaration lint + AST no-bypass lint + inflation lint, all manifest-derivable / call-graph-static; architecturally_distinct: GO3B1-01 mandates the field exists, this makes it a binding SDK-applied un-bypassable cap — different failure mode, disjoint artefacts; in_bp_scope: BP literally "Control model response length"; not_vendor_menu: Bedrock inference-parameters / stop sequences stripped to framework_mappings + Option B, generic declared-cap mandate survives). SOLUTION: Option A (declared-and-bound, manifest-derivable) the mandated spine; Options B (declared stop_sequences) + C (runtime alarm on GO3B2-01's output-token signal) documented as additive alternative RIs; RI + gates built on Option A only. Fields: pillar P5 — Cost Optimization; focus_area P52 — Cost-aware Prompting (shared with GC3B1-01 / GC3B3-01 — the prompt-cost triad: input footprint / output cap / cache reuse); impact_level Medium (matches BP risk, recoverable via capping); applicability { llm/rag/agentic mandatory, ml nice_to_have }; serving_paradigm all four mandatory (response length drives cost under per-token billing AND self-hosted compute time); maturity_level scaling (sits on GO3B1-01's scaling-tier registry); ownership enterprise (D1=no / D2=2 — declaration + AST + governance lints in the central llm-sdk suite, thinner than GC3B1-01 as it adds no new tokenizer computation, just extends GO3B1-01's no-inline AST lint with one parameter) / project_architect / self_attestation_with_mechanical_evidence / dashboard_and_spot_check; dependencies [{ GO3B1-01 hard }]; references TWO real (Amazon Bedrock inference-parameters max_tokens doc; Stackviv max-tokens/stop-sequences article) — lighter than the sibling's five, flagged to enrich; AIGP III.B unverified. RI at data/ri/GC3B2-01/README.md (Option A). OPEN ITEMS: (1) JSON parse — run in-session (count 23, parses OK). (2) framework_mappings.aws mapping_state **unverified** — confirm the BP's numbered implementation steps from the live page (couldn't fetch this session — docs.aws.amazon.com timeouts) and promote to verified. (3) references light (2 vs 5) — enrich with named-company output-cost cases. (4) S3 re-upload + frontend build. **GENCOST03 fully walked + CLOSED: BP01 GC3B1-01, BP02 GC3B2-01, BP03 GC3B3-01, BP04 not_promoted (3 promoted, 1 not_promoted).** See decisions.md (latest).
