# GENPERF04-BP02

- **Key:** `GENPERF04-BP02`
- **Date:** 2026-06-07
- **Outcome:** not_promoted

---

- 2026-06-07 | GENPERF04-BP02 (whole BP — "Optimize vector sizes for your use case"; AWS verbatim fetched 2026-06-07, 3 steps, risk **Low**) | not_promoted / absorbed into GC4B1-01 — no principle authored. The BP is experiment-and-pick: step 1 identify the workload's most important performance KPI (accuracy/speed/memory/scalability); step 2 determine the vector options the embedding model supports and design experiments to test each (sub: experiment over varied data; self-host an open-source embedding model via SageMaker if options insufficient); step 3 run the experiment and pick the most performant embedding model. Walked interactively with the user 2026-06-07; **user directed not_promote and absorb.** Two grounds. (1) **No enforceable artefact distinct from GC4B1-01's.** The only commitable, gateable thing in this BP is the same declaration GC4B1-01 already mandates — `vectorstore/embedding.yaml` carrying `embedding_model` + `vector_dimension` + the measured retrieval-quality result (`quality_floor` / `quality_result`) that justified the chosen dimension, gated pre-merge. GENPERF04-BP02 wants that exact declaration; it just frames the justifying number as a latency/accuracy KPI rather than a cost-vs-quality floor. The BP's own steps are decision-process advice ("identify your KPI") + experimentation ("design experiments", "run them") with no standalone pre-merge artefact — the same shape as GC4B1-01's own steps 3/4, which were not_promoted for having "no commitable pre-merge artefact distinct from the quality-floor number the declaration records" and were explicitly earmarked as landing in GENPERF04. The substance landing here does not make it newly promotable. (2) **Architecturally redundant with GC4B1-01.** step_promotion would fail on has_enforceable_artefact (~1: identify-KPI is decision-process advice à la GENCOST01-BP01 step 1; design/run-experiment is process) and architecturally_distinct (~1-2: the declare-dimension-plus-measured-evidence artefact is identical to GC4B1-01's). in_bp_scope 3 (GENPERF, vector-store optimization) and not_vendor_menu ~2-3 (mostly generic, light SageMaker lean) do not rescue the call. The honest promote case considered and rejected: GC4B1-01 is cost-framed with a smaller-is-better bias (`quality_floor` is a floor → go as small as clears it), whereas a latency- or accuracy-critical workload optimizes for a named KPI where the best size may not be the smallest; one could carve a performance principle requiring the deployed dimension to be the recorded winner of a KPI experiment matrix (size → accuracy/latency/memory). But that experiment-matrix-as-artefact is precisely the process-shaped thing already judged ungateable in GC4B1-01 steps 3/4, and AWS rates this BP **Low** (lowest rating). **Absorption:** GC4B1-01's declaration is the home for the performance-KPI evidence — its `quality_floor`/`quality_result` fields should be read to cover the workload's named performance KPI (accuracy/latency/memory), not only the cost-vs-quality floor; GENPERF04-BP01 (test vector embeddings for latency — UNMAPPED) + BP02 together are the performance-testing context around that one declaration. step_promotion not formally re-scored — the user made the call directly; rubric outcome would be not_promote on has_enforceable_artefact + architecturally_distinct grounds. **GENPERF04 status:** BP01 UNMAPPED (latency-testing, process-shaped — likely the same absorb/not_promote shape); BP02 not_promoted/absorbed into GC4B1-01.
