# GENPERF01-BP02

- **Key:** `GENPERF01-BP02`
- **Date:** 2026-06-07
- **Outcome:** not_promoted

---

- 2026-06-07 | GENPERF01-BP02 (whole BP — "Collect performance metrics from generative AI workloads"; AWS verbatim fetched 2026-06-07, 5 steps, risk **Medium**) | not_promoted / absorbed by GO3B2-01 + the GO1B1 family. The BP directs teams to identify priority performance metrics, capture them (OpenLLMetry trace framework / CloudWatch / fmeval), establish thresholds and alert (CloudWatch alarms on latency/throughput + regular golden-dataset benchmarking), log remediation actions in the org AI policy, and run centralized experiment tracking (SageMaker + MLflow). **User directed not_promote 2026-06-07.** It decomposes entirely into already-owned or generic substance: "report metrics/telemetry/logs to centralized monitoring + implement a trace framework like OpenLLMetry" = **GO3B2-01** (the observability emission contract — observability SDK as the only emit path); "evaluate response quality over time / regular golden-dataset benchmarking" = the GO1B1 eval harness on a cadence + **GO1B1-04** drift monitoring; "CloudWatch alarms on latency/throughput" = generic infrastructure observability the base WAF owns (the BP cross-references `PERF05-BP01/02/03/05` and six `MLPER` items); "remediation actions in the AI policy" + "MLflow experiment tracking" = process/vendor advice. The honest promote case — a "latency/throughput SLO-as-code wired to alerts" principle analogous to GO1B1-03's metric-as-code — was considered and rejected: it is the same alert-threshold substance not_promoted at GENPERF02-BP01, overlaps GO3B2-01, and latency SLOs + alarms are generic infra discipline (base WAF PERF05), so nothing GenAI-distinct survives. step_promotion would fail architecturally_distinct + not_vendor_menu (OpenLLMetry/CloudWatch/fmeval/MLflow menu). **GENPERF01 focus area CLOSED** (two BPs, both not_promoted). **Pillar note: GENPERF fully walked and yields ZERO principles** — every GENPERF BP resolved to either generic infrastructure discipline owned by the base Well-Architected framework (PERF05 / ML lens MLPER) or a performance-framed restatement of an already-authored GENOPS evaluation (GO1B1) or observability (GO3B2) principle, or a vendor menu. The GenAI performance pillar adds no enforceable architecture beyond what GENOPS evaluation + observability already mandate.
