# GENREL01-BP01

- **Key:** `GENREL01-BP01`
- **Date:** 2026-06-22
- **Outcome:** not_promoted

---

- 2026-06-22 | GENREL01-BP01 (whole BP — "Scale and balance foundation model throughput as a function of utilization"; focus-area + BP title confirmed via search, live BP page not fetchable this session — web_fetch timed out, no Chrome connected; mapping_state **unverified**; GENREL01 ships one BP per search enumeration, confirm on a verification pass) | **not_promoted (user-directed 2026-06-22).** First GENREL focus area walked since GENREL03; opens the rest of the Reliability pillar. **The problem:** foundation-model APIs cap requests/tokens per minute; on Bedrock that quota is per account/region and shared by every workload on the account, so one workload's spike throttles the others (multiple keys in one account do NOT split the quota — Bedrock quotas are account-scoped). **The HALT** (extended, user drove it): user pressed on enforceability exactly as in the GC3B2-01 walk. Walked through (1) a client-side rate limiter — rejected: it runs per process, so N instances each capped at X sum to N·X and never protect a *shared* quota; (2) a central rate-limiting gateway with global token-bucket state — rejected as a parallel backend the org won't build, and per-client quota allocation is an operational/political problem not an architectural one; (3) the user's own proposal — per-project keys/accounts for isolated quota + per-project billing — accepted as the clean answer, but it resolves to **generic multi-account landing-zone governance (base-WAF), not an AI-specific principle.** Once isolation + provisioned throughput do the work, nothing distinct and enforceable survives. **Absorption:** throttling recovery (429 retry+backoff) → **GR3B1-01**; cross-region throughput balancing → **GENREL05-BP01** (separate focus area, walked later); provisioned-vs-on-demand capacity = a cost/buy decision adjacent to **GC2B2-01** (hosting right-sizing), not a gate; generic autoscaling → base-WAF REL + GENSUS01-BP01 (Sustainability, out of scope). step_promotion **3/3/3/3 → not_promote** (has_enforceable_artefact 3 — correctly identifies the rate-limiter can't protect a shared quota and the gateway won't be built; architecturally_distinct 3 — GR3B1-01 / GENREL05-BP01 / GC2B2-01 named precisely; in_bp_scope 3 — native GENREL01, surviving enforcement is base-WAF multi-account, named out-of-catalogue; not_vendor_menu 3 — Bedrock provisioned-throughput stripped, only capacity-planning advice survives). **DELIVERABLE: ADR at data/adr/GENREL01-throughput-quotas.md** — records the recommendation given to projects instead of a principle: (1) isolate by default — each project its own provider account/key → own quota + per-project bill; (2) reserve capacity for critical steady traffic (Bedrock Provisioned Throughput / committed tiers), bursty stays on-demand; (3) each project stays under its own ceiling via client-side rate limit + GR3B1-01 backoff; (4) no central gateway / no quota pooling; (5) central team ships a *standard* (declare provider + capacity mode + rate-limit config through the shared SDK), not a backend; monitoring rides GO3B2-01. **Also resolves the deferred GENOPS02-BP03 provider-quota slice** — it lands here and likewise resolves to account isolation + GO3B2-01 monitoring, no new gate. First time the catalogue has produced an ADR as the deliverable of a not_promote (new `data/adr/` folder). No principle authored; catalogue stays at 23 standards.
