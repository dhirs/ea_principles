# GENCOST05-BP01 step 2

- **Key:** `GENCOST05-BP01 step 2`
- **Date:** 2026-06-06
- **Outcome:** promoted → GC5B1-01

---

- 2026-06-06 | GENCOST05-BP01 step 2 (AWS verbatim "Implement stopping conditions that enable an agent to run to the maximum duration. Stopping conditions may be a timeout mechanism like the one in Amazon Bedrock. Alternatively, stopping conditions may be implemented in the prompt flow layer or within a software abstraction layer.") | promoted_to_principle: GC5B1-01 — Give every agent a hard stop. First principle under GENCOST05 / GenCost Q5; first in the new focus area P54 — Cost-informed Agents. 15th principle in the catalogue (9 GENOPS + 6 GENCOST). GENCOST05 ships exactly ONE BP (BP01); focus area CLOSED. AWS risk **High** — the first GENCOST BP above Medium. Concretises the advice ("create stopping conditions") into an enforceable contract: a declared per-agent run-limits record (`max_steps` + `max_wallclock_seconds` + `max_run_token_budget` + `on_limit` + `estimated_max` + `checked_on` in `agent/run_limits.yaml`) + an `agent/stop_check` lint enforcing (1) declaration completeness, (2) wired-in consistency by parsing the agent's orchestration-framework construction (LangGraph `recursion_limit` / CrewAI `max_iter`+`max_execution_time` / Bedrock agent timeout / custom loop guard) so a declared-but-unbound limit fails, (3) ceiling-relaxation governance requiring a recorded cost rationale for any upward change. The gate enforces ceilings are declared and structurally wired in; it does NOT prove the agent halts at the budget under live load (runtime behaviour). Option B (runtime circuit-breaker: a cost/step/duration meter that hard-stops a run on ceiling breach and emits to observability — GO3B2 territory) documented as an additive alternative; RI and gates built on Option A. step_promotion 3/3/3/3 (enforceable declaration + wired-in/governance gates; architecturally distinct — the cost lever is the NUMBER OF STEPS and the DURATION of an agent run, not a per-call `max_tokens` cap (GO3B1-01) nor a prompt-token footprint (GC3B1-01) nor vector-store dimension (GC4B1-01); BP literally "Create stopping conditions to control long-running workflows" under GENCOST05 "Cost-informed agents"; the Bedrock timeout is ONE option among the explicit generic alternatives "prompt flow layer or software abstraction layer", so a vendor-neutral mandate survives). dependencies: [] standalone (the per-call `max_tokens` substrate GO3B1-01 is adjacent but not a substrate dependency — this bounds run length, not per-call output). references (schema v1.11): DEV Community/Waxell (four LangChain agents looping eleven days for ~$47,000; alerts-are-not-enforcement) + n1n.ai (~$48,000 in fourteen hours from one session; no per-agent ceiling / no termination root cause). ownership.tier enterprise via D1=no / D2=2 (declaration lint + framework-aware wired-in parser across LangGraph/CrewAI/Bedrock/custom + relaxation-governance lint, lighter than GC2B2-01's multi-cloud IaC parse) → recommended_centralise; validator project_architect, audit_mode self_attestation_with_mechanical_evidence. applicability { agentic mandatory } ONLY — llm/rag/ml OMITTED (no agent loop to bound on a single-shot call, a fixed RAG pipeline, or classical ML; parallel to GO1B1-01/GO1B1-05 agentic-only and GC4B1-01's narrow-by-omission precedent). serving_paradigm all four mandatory (a runaway loop bills regardless of serving paradigm). maturity_level **foundational** — the gate reads the agent's own construction code in-repo (no deployed artefact/baseline/telemetry), and a single unbounded run can be catastrophic on the first production session (AWS High; cited incidents five/six-figure single events), so it pays off at project #1; principled outlier from the cost siblings GC4B1-01 (scaling) / GC2B2-01 (mature) which parse DEPLOYED resources. impact_level **High** — matches AWS BP01's High rating (first GENCOST principle at High; the others are Medium) and the spike-shaped failure (cost predictability is the BP's whole desired outcome). STATEMENT TITLE kept deliberately plain per the user's working-style rule in data/CLAUDE.md — trades the statement rubric's names_artefact_and_enforcement down (scored 1) in favour of the override; artefact + enforcement carried in statement.description. AIGP III.B unverified. RI at data/ri/GC5B1-01/README.md (Option A). Step 1 absorbed; step 3 not_promoted.
