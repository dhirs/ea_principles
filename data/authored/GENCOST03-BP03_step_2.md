# GENCOST03-BP03 step 2

- **Key:** `GENCOST03-BP03 step 2`
- **Date:** 2026-06-05
- **Outcome:** promoted → GC3B3-01

---

- 2026-06-05 | GENCOST03-BP03 step 2 (AWS verbatim "Enable prompt caching for supported models: Turn on caching in Amazon Bedrock console; For APIs, set appropriate caching flags; Configure cache checkpoints at optimal locations") | promoted_to_principle: GC3B3-01 — Mark the cacheable static prefix of every reused prompt template and fail builds that leave a cache-eligible template uncached or mis-declare its prefix size. Second principle under GENCOST03 / P52 Cost-aware Prompting (sibling to GC3B1-01). Concretises the Bedrock-flavoured step into a provider-neutral mandate: declare `cache.static_prefix_tokens` + a cache decision (checkpoint or opt_out) in the GO3B1-01 manifest; a `prompts/cache_check` lint (1) requires the declaration, (2) requires cache-eligible templates (declared prefix ≥ the model minimum in `models/cache_minimums.yaml`) to be checkpointed or opted out, (3) recomputes the contiguous static prefix with the model tokenizer and fails on >10% variance. Manifest-derivable Option A is the mandated spine; Option C (runtime telemetry alarm on cache hit-rate, via GO3B2-01's stream) documented as an additive alternative realising step 3's monitoring. Distinct from GC3B1-01: opposite cost lever (GC3B1-01 fails prompts too BIG; GC3B3-01 caches big STABLE reused prefixes), disjoint gates. step_promotion 3/3/3/3 (enforceable manifest artefact + recompute gate; architecturally distinct sibling; BP literally "Implement prompt caching to reduce token costs"; Bedrock console/checkpoint specifics stripped, the mark-the-stable-prefix-as-a-checkpoint mandate survives — Anthropic cache_control / OpenAI prefix caching / Bedrock checkpoints all implement it). dependencies: GO3B1-01 hard (registry + manifest + tokenizer), GC3B1-01 soft (tokenizer-integration pattern). ownership.tier enterprise via D1=no / D2=2 → recommended_centralise. serving_paradigm api_on_demand + api_provisioned mandatory, self-hosted nice_to_have (reduced-cached-rate billing is a managed-API feature; self-hosted benefit is KV-cache recompute/latency, not a per-token bill line) — a deliberate departure from GC3B1-01's all-four-mandatory. maturity_level scaling (sits on GO3B1-01 substrate; pays off project #2). impact Medium. AIGP III.B unverified. RI at data/ri/GC3B3-01/README.md (Option A). Step 1 absorbed.
