# GENREL06-BP01

- **Key:** `GENREL06-BP01`
- **Date:** 2026-06-27
- **Outcome:** not_promoted

---

- 2026-06-27 | GENREL06-BP01 (whole BP — "Design for fault-tolerance for high-performance distributed computation tasks"; GENREL06 ships ONE BP; live pages fetched + verified 2026-06-27 from genrel06.html + genrel06-bp01.html; mapping_state **verified**) | **not_promoted (user-directed 2026-06-27).** Last BP of the Reliability pillar (GENREL06 — Distributed compute tasks), AWS risk **High**. The BP is entirely about **model customization** — pre-training, continuous pre-training, fine-tuning, distillation: long-running (days–months) distributed training jobs across dozens/hundreds of VMs, made fault-tolerant via checkpointing to S3/FSx for Lustre, auto-resume from last checkpoint, GPU/node health monitoring (DCGM, EFA health checks) and automatic faulty-node replacement, on SageMaker AI HyperPod (EKS/Slurm) / Amazon Bedrock managed customization / AWS Batch. **No distinct enforceable AI-specific principle survives — the cleanest not_promote of the pillar.** (1) **Out of the dominant serving paradigm** — this only bites teams that *self-host model training/customization*; the catalogue's primary audience consumes foundation models via managed API and never customizes one, so there is nothing to gate (same serving-paradigm boundary the catalogue uses elsewhere); (2) **generic distributed-HPC fault tolerance** — checkpoint / retry / replace-node is standard long-running-job resilience, "model customization job" standing in for any HPC job; the BP's own Resources cross-reference base-WAF **REL10-BP02**, **REL11-BP01**, **REL11-BP03**; (3) **vendor menu** (HyperPod / Bedrock / Batch / FSx for Lustre / EKS / Slurm), the not_vendor_menu failure shape; (4) **no commitable workload-repo artefact or CI gate** — checkpointing/auto-resume live in training scripts + cluster config, infra-level, not a lint on an agent/RAG workload repo. Consistent with how the catalogue already treats training: **GENOPS04** closed partly on the FMOps training-pipeline vendor menu, and model-customization *cost* was punted to **GENSUS01-BP02** (out of the current walk). Training infrastructure is out of the catalogue's remit. step_promotion **3/3/3/3 → not_promote** (has_enforceable_artefact 3 — correctly identifies no workload-repo artefact, training config is infra; architecturally_distinct 3 — generic distributed-HPC redundant with base-WAF REL10/REL11, named; in_bp_scope 3 — native GENREL06 but substance is base-WAF + out-of-serving-paradigm; not_vendor_menu 3 — HyperPod/Bedrock/Batch/FSx menu quoted, nothing architectural survives). No ADR (like GENREL02 — no AI-specific recommendation to record beyond "use managed training + checkpoint"; guidance is base-WAF REL10/REL11). No principle authored; catalogue stays at 25 standards. **Closes GENREL06 and the entire Reliability pillar.**
