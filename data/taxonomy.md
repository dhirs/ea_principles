# DEPRECATED — moved to `taxonomy.json`

This file has been converted to structured JSON.

**Use `taxonomy.json` instead.**

The conversion happened on 2026-05-26. All content from this file — the five-pillar taxonomy, focus areas, principle schema specification, framework_mappings spec, ownership tiers, gates and evidence specs, change_history spec, enums, and conventions — is preserved in `taxonomy.json` in structured form. The historical "Why the `enforcement` field was retired" section was dropped (already captured in `decisions.md` under the 2026-05-24 schema-v1.2 entry).

This `.md` file remains only as a deprecation marker so any reader following an outdated pointer is redirected. It can be deleted; tooling and other catalogue files now point at `taxonomy.json`.

See the 2026-05-26 entry in `decisions.md` for full context on the conversion.
